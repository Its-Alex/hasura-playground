table:
  name: users
  schema: public
array_relationships:
  - name: users_tags
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: users_tags
          schema: public
