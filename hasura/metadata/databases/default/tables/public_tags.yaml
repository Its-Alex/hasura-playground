table:
  name: tags
  schema: public
array_relationships:
  - name: users_tags
    using:
      foreign_key_constraint_on:
        column: tag_id
        table:
          name: users_tags
          schema: public
